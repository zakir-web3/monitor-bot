name: Monitor GitHub Releases

on:
  schedule:
    - cron: '18 0 * * *'  # daily at 08:18 CST
  workflow_dispatch:       # allow manual trigger

concurrency:
  group: monitor
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.23'

      - name: Run monitor
        env:
          GH_PAT_CLASSIC_TOKEN: ${{ secrets.GH_PAT_CLASSIC_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: go run .

      - name: Commit updated version
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add last_versions.json
          git diff --staged --quiet || (git commit -m "chore: update last known version" && git push)
